spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:postgres;DB_CLOSE_DELAY=-1
  flyway:
    enabled: false
  h2:
    console: 
      enabled: true
  jpa:
    hibernate:
      ddl-auto: none

swagger:
  api-version: 1.0
  title: User API
  description: Documentation User API v${swagger.api-version}